name: Deploy Next.js to GitHub Pages

on:
  # Trigger on push to main branch
  push:
    branches: [ main ]
  
  # Allow manual trigger from Actions tab
  workflow_dispatch:

# Set permissions for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write
  actions: read
  deployments: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Debug environment variables
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
          NEXT_PUBLIC_API_BASE_URL_VAR: ${{ vars.NEXT_PUBLIC_API_BASE_URL }}
        run: |
          echo "🔍 DEBUG: Secret NEXT_PUBLIC_API_BASE_URL = $NEXT_PUBLIC_API_BASE_URL"
          echo "🔍 DEBUG: Variable NEXT_PUBLIC_API_BASE_URL = $NEXT_PUBLIC_API_BASE_URL_VAR"
          echo "🔍 DEBUG: Todas las variables de entorno:"
          env | grep NEXT_PUBLIC_ || echo "❌ No se encontraron variables NEXT_PUBLIC_"
          echo "🔍 DEBUG: Secrets disponibles:"
          echo "- NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-'NO DEFINIDO'}"
        
      - name: Build Next.js static export
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL || vars.NEXT_PUBLIC_API_BASE_URL }}
        run: |
          echo "🚀 BUILD: Usando NEXT_PUBLIC_API_BASE_URL = $NEXT_PUBLIC_API_BASE_URL"
          npm run build
        
      - name: Add .nojekyll file
        run: touch out/.nojekyll
        
      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
